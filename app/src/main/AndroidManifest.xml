<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.home.junoon">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity android:name=".updateActivity" />
        <activity android:name=".DetailsActivity" />
        <activity android:name=".MainMenuActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".IncomeFieldActivity" />
        <activity android:name=".AcademicFieldActivity" />
        <activity android:name=".StudentDetails" />
        <activity android:name=".SpecificStudentActivity" />
        <activity android:name=".uGeneralDetailsActivity" />
        <activity android:name=".uAcademicActivity" />
        <activity android:name=".uIncomeActivity"></activity>
    </application>

</manifest>